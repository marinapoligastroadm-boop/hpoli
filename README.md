# HPOLI Faturamento

Aplicação de gestão de faturamento do HPOLI Centro Médico, com autenticação e dados persistidos no Supabase.

## Desenvolvimento

```bash
npm install
npm run dev
```

Crie um arquivo `.env.local` com as variáveis descritas em `.env.example`.
